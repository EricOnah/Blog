<%- include('partials/header'); %>

    <div class="posts container">
        <h1 class="page-title mb-5">Home</h1>
        <p>
            <%= homeStartingContent %>
        </p>
        <% posts.forEach((post)=> { %>
            <h1 class="page-title">
                <%= post.title %>
            </h1>

            <% let content=post.content.slice(0, 100) %>
                <p>
                    <%= content %>...
                        <a href="/posts/<%= post.content %>">Read More</a </p>
                        <% }) %>
    </div>


    <%- include('partials/footer'); %>